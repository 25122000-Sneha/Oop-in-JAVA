//Checked exceptions are checked at compile time.
import java.io.*;
class CheckedException
{
        public static void main()
        {
                FileReader file = new FileReader("stars.txt");
        }
}